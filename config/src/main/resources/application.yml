server:
  port: 9000

spring:
  application:
    name: config-service

  cloud:
    config:
      server:
        git:
          uri: git@github.com:Hong-mae/spring-msa-prj-config.git
#          clone-on-start: true